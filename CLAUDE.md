# Jojo Casting

Application de direction de casting (React 18, fichier unique `src/App.jsx`, Vite, Supabase, déploiement Vercel).

## Règles

- **Toute modification UI doit respecter DESIGN.md.**
- Build : `npx vite build` · Dev : serveur `jojo-dev` (port 5173, voir `.claude/launch.json`)
- Commits : `git -c user.name="Lenny" -c user.email="lg@jojo.com" commit …`
